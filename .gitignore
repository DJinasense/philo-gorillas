.claude/settings.local.json
node_modules/
.vercel
.DS_Store

# Local notes that hold real API keys. GitHub push protection caught these once
# already — keep them out of the index so a stray `git add -A` can't stage them.
IPA*.txt
*secret*.txt
*keys*.txt
.env
.env.*

.vercel
.env*

# Raw ebook/audio/video library — source material for scripts/ingest/*.py.
# Only the extracted plain-text output (data/texts/*.txt) belongs in git.
TEXTS/
raw/

# hyperresearch tool state + fetched research notes — local working data
# (may include copyrighted excerpts pulled from the web); this repo is public.
.hyperresearch/
research/
